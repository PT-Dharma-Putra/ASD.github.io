<section
  id="home"
  class="relative min-h-screen flex flex-col justify-between overflow-hidden"
>
  <div class="absolute inset-0 z-0">
    <img
      alt="Luxury Interior Abhijati Sahar Dharma"
      class="w-full h-full object-cover scale-105 animate-slow-zoom"
      src="/images/Hero section.png"
    />
    <div
      class="absolute inset-0 bg-gradient-to-b from-black/75 via-black/50 to-black/20"
    >
    </div>
  </div>

  <!-- Top spacer to account for the navbar height -->
  <div class="h-24 md:h-28"></div>

  <div class="relative z-10 w-full max-w-5xl mx-auto text-center text-white px-6 my-auto">
    <h1
      class="font-display text-5xl md:text-7xl lg:text-8xl font-bold mb-6 tracking-tight leading-[1.1] animate-fade-in-up"
    >
      INTERIOR &<br />
      <span class="text-gradient-gold italic"> FURNITURE SOLUTION </span>
    </h1>

    <p
      class="text-lg md:text-2xl text-white/90 mb-10 max-w-2xl mx-auto font-light tracking-wide leading-relaxed animate-fade-in-up delay-200"
    >
      Creating Spaces For Better Care
    </p>

    <div
      class="flex flex-col sm:flex-row gap-5 justify-center items-center animate-fade-in-up delay-500"
    >
      <a
        href="#projects"
        class="group relative overflow-hidden bg-white/10 hover:bg-white/20 backdrop-blur-md border border-white/20 text-white font-bold py-4 px-10 rounded-full transition-all duration-300 tracking-[0.15em] uppercase text-[0.7rem]"
      >
        <span class="relative z-10">Explore Portfolio</span>
        <div
          class="absolute inset-0 bg-gradient-to-r from-transparent via-white/10 to-transparent -translate-x-full group-hover:translate-x-full transition-transform duration-1000"
        >
        </div>
      </a>
      <a
        href="https://wa.me/6281127005688"
        class="bg-primary hover:bg-primary/90 text-white font-bold py-4 px-10 rounded-full transition-all duration-300 shadow-xl shadow-primary/20 hover:shadow-primary/40 transform hover:-translate-y-1 active:scale-95 tracking-[0.15em] uppercase text-[0.7rem]"
      >
        Start a Project
      </a>
    </div>
  </div>

  <!-- Bottom Features Bar -->
  <div class="relative z-20 w-full bg-white border-t border-slate-100 shadow-2xl py-6 md:py-8 mt-12">
    <div class="max-w-7xl mx-auto px-6 grid grid-cols-1 md:grid-cols-3 gap-6 md:gap-12 items-center">
      <!-- Item 1 -->
      <div class="flex items-center gap-4 justify-center md:justify-start group transition-all duration-300">
        <div class="p-3 bg-primary/5 rounded-xl text-primary transition-all duration-300 group-hover:bg-primary group-hover:text-white shadow-sm">
          <svg class="w-8 h-8 transition-transform duration-300 group-hover:scale-110" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>
            <path d="M9 11l2 2 4-4"/>
            <path d="M19 3h.01M22 6h.01M16 5h.01"/>
          </svg>
        </div>
        <div>
          <h3 class="font-display font-bold text-secondary text-sm md:text-base leading-tight uppercase tracking-wider">
            Hygienic & Durable
          </h3>
          <p class="text-xs text-slate-500 font-light mt-0.5">
            Hospital-grade & long-lasting materials
          </p>
        </div>
      </div>

      <!-- Item 2 -->
      <div class="flex items-center gap-4 justify-center group transition-all duration-300">
        <div class="p-3 bg-primary/5 rounded-xl text-primary transition-all duration-300 group-hover:bg-primary group-hover:text-white shadow-sm">
          <svg class="w-8 h-8 transition-transform duration-300 group-hover:scale-110" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10" stroke-dasharray="2 2" />
            <circle cx="12" cy="5" r="2.5" fill="currentColor" />
            <circle cx="6" cy="15.5" r="2.5" fill="currentColor" />
            <circle cx="18" cy="15.5" r="2.5" fill="currentColor" />
            <path d="M12 7.5v5M6.5 14l4-2M17.5 14l-4-2" />
          </svg>
        </div>
        <div>
          <h3 class="font-display font-bold text-secondary text-sm md:text-base leading-tight uppercase tracking-wider">
            Ergonomic Design
          </h3>
          <p class="text-xs text-slate-500 font-light mt-0.5">
            Optimized for comfort & function
          </p>
        </div>
      </div>

      <!-- Item 3 -->
      <div class="flex items-center gap-4 justify-center md:justify-end group transition-all duration-300">
        <div class="p-3 bg-primary/5 rounded-xl text-primary transition-all duration-300 group-hover:bg-primary group-hover:text-white shadow-sm">
          <svg class="w-8 h-8 transition-transform duration-300 group-hover:scale-110" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round">
            <rect x="3" y="4" width="18" height="12" rx="2"/>
            <path d="M2 18h20c1 0 2-.5 2-1v-1H0v1c0 .5 1 1 2 1z"/>
            <path d="M15.5 6.5l2 2-7.5 7.5H8v-2l7.5-7.5z"/>
          </svg>
        </div>
        <div>
          <h3 class="font-display font-bold text-secondary text-sm md:text-base leading-tight uppercase tracking-wider">
            Custom Solution
          </h3>
          <p class="text-xs text-slate-500 font-light mt-0.5">
            Tailored to your specific space needs
          </p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  @keyframes slow-zoom {
    0% {
      transform: scale(1);
    }
    100% {
      transform: scale(1.1);
    }
  }

  @keyframes fade-in-up {
    0% {
      opacity: 0;
      transform: translateY(30px);
    }
    100% {
      opacity: 1;
      transform: translateY(0);
    }
  }

  @keyframes fade-in {
    0% {
      opacity: 0;
    }
    100% {
      opacity: 1;
    }
  }

  .animate-slow-zoom {
    animation: slow-zoom 20s ease-out forwards;
  }

  .animate-fade-in-up {
    animation: fade-in-up 1s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    opacity: 0;
  }

  .animate-fade-in {
    animation: fade-in 1.5s ease-out forwards;
  }

  .delay-200 {
    animation-delay: 200ms;
  }

  .delay-500 {
    animation-delay: 500ms;
  }
</style>
