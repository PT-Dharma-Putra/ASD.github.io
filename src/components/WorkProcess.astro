---

---

<section
  id="process"
  class="relative py-24 lg:py-32 overflow-hidden bg-white border-t border-slate-50"
>
  <!-- Atmospheric Background -->
  <div class="absolute inset-0 z-0 opacity-10 pointer-events-none">
    <div
      class="absolute inset-0 bg-[radial-gradient(circle_at_center,rgba(228,34,46,0.08)_0%,transparent_70%)]"
    >
    </div>
    <div
      class="absolute inset-0 bg-[radial-gradient(#e4222e10_1.5px,transparent_1.5px)] [background-size:32px_32px] opacity-20"
    >
    </div>
  </div>

  <div class="max-w-7xl mx-auto px-6 relative z-10">
    <!-- Section Header -->
    <div class="text-center mb-24 reveal">
      <div
        class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-primary/10 border border-primary/20 backdrop-blur-md mb-6"
      >
        <span class="w-1.5 h-1.5 rounded-full bg-primary animate-pulse"></span>
        <span
          class="text-[9px] font-bold tracking-[0.3em] text-primary uppercase"
          >Execution Blueprint</span
        >
      </div>
      <h2
        class="font-display text-4xl md:text-6xl text-slate-800 font-bold tracking-tight mb-6"
      >
        Our Work <span class="italic text-primary">Process</span>
      </h2>
      <div class="w-20 h-1 bg-primary mx-auto rounded-full"></div>
    </div>

    <!-- Process Flow Container -->
    <div class="relative w-full max-w-6xl mx-auto">
      <!-- Desktop Connectors (Hidden on Mobile) -->
      <div class="hidden lg:block absolute inset-0 pointer-events-none z-0">
        <svg
          class="w-full h-full"
          preserveAspectRatio="none"
          viewBox="0 0 1000 800"
        >
          <!-- Explicit Connector Arrows (Dashed Lines Only) -->
          <path
            d="M300 120 L700 120"
            fill="none"
            stroke="rgba(228,34,46,0.8)"
            stroke-width="3"
            stroke-dasharray="10 10"
            class="path-animate"></path>
          <path
            d="M850 180 L850 420"
            fill="none"
            stroke="rgba(228,34,46,0.8)"
            stroke-width="3"
            stroke-dasharray="10 10"
            class="path-animate"></path>
          <path
            d="M700 480 L300 480"
            fill="none"
            stroke="rgba(228,34,46,0.8)"
            stroke-width="3"
            stroke-dasharray="10 10"
            class="path-animate"></path>
          <path
            d="M150 540 L150 780"
            fill="none"
            stroke="rgba(228,34,46,0.8)"
            stroke-width="3"
            stroke-dasharray="10 10"
            class="path-animate"></path>
          <path
            d="M300 780 L700 780"
            fill="none"
            stroke="rgba(228,34,46,0.8)"
            stroke-width="3"
            stroke-dasharray="10 10"
            class="path-animate"></path>
        </svg>
      </div>

      <!-- Steps Grid -->
      <div class="space-y-16 lg:space-y-32 relative">
        <!-- Row 1 (1-2) -->
        <div
          class="flex flex-col lg:flex-row justify-between gap-12 lg:gap-24 relative"
        >
          <!-- Step 01 -->
          <div class="flex items-center w-full lg:w-[45%] relative reveal">
            <div
              class="group relative w-full p-8 lg:p-10 bg-white border border-slate-100 rounded-[2rem] shadow-[0_10px_40px_-15px_rgba(0,0,0,0.05)] transition-all duration-500 hover:border-primary/40 hover:shadow-2xl"
            >
              <span
                class="absolute top-4 right-8 text-6xl font-display font-bold text-primary/5 group-hover:text-primary transition-colors"
                >01</span
              >
              <div class="relative z-10">
                <div
                  class="w-14 h-14 rounded-2xl bg-slate-50 border border-slate-100 flex items-center justify-center mb-6 shadow-sm group-hover:bg-primary/10 transition-all"
                >
                  <span class="material-icons-outlined text-3xl text-primary"
                    >forum</span
                  >
                </div>
                <h3 class="font-display text-2xl font-bold text-slate-800 mb-4">
                  Consultation
                </h3>
                <p class="text-slate-500 text-sm leading-relaxed">
                  Deep-dive technical discovery to align vision, budget, and
                  project milestones.
                </p>
              </div>
            </div>
          </div>
          <!-- Step 02 -->
          <div class="flex items-center w-full lg:w-[45%] relative reveal">
            <div
              class="group relative w-full p-8 lg:p-10 bg-white border border-slate-100 rounded-[2rem] shadow-[0_10px_40px_-15px_rgba(0,0,0,0.05)] transition-all duration-500 hover:border-primary/40 hover:shadow-2xl"
            >
              <span
                class="absolute top-4 right-8 text-6xl font-display font-bold text-primary/5 group-hover:text-primary transition-colors"
                >02</span
              >
              <div class="relative z-10">
                <div
                  class="w-14 h-14 rounded-2xl bg-slate-50 border border-slate-100 flex items-center justify-center mb-6 shadow-sm group-hover:bg-primary/10 transition-all"
                >
                  <span class="material-icons-outlined text-3xl text-primary"
                    >design_services</span
                  >
                </div>
                <h3 class="font-display text-2xl font-bold text-slate-800 mb-4">
                  Design & Planning
                </h3>
                <p class="text-slate-500 text-sm leading-relaxed">
                  Architectural blueprinting and 3D visualization to perfect
                  every structural detail.
                </p>
              </div>
            </div>
          </div>
        </div>

        <!-- Row 2 (3-4) -->
        <div
          class="flex flex-col lg:flex-row-reverse justify-between gap-12 lg:gap-24 relative"
        >
          <!-- Step 03 -->
          <div class="flex items-center w-full lg:w-[45%] relative reveal">
            <div
              class="group relative w-full p-8 lg:p-10 bg-white border border-slate-100 rounded-[2rem] shadow-[0_10px_40px_-15px_rgba(0,0,0,0.05)] transition-all duration-500 hover:border-primary/40 hover:shadow-2xl"
            >
              <span
                class="absolute top-4 right-8 text-6xl font-display font-bold text-primary/5 group-hover:text-primary transition-colors"
                >03</span
              >
              <div class="relative z-10">
                <div
                  class="w-14 h-14 rounded-2xl bg-slate-50 border border-slate-100 flex items-center justify-center mb-6 shadow-sm group-hover:bg-primary/10 transition-all"
                >
                  <span class="material-icons-outlined text-3xl text-primary"
                    >category</span
                  >
                </div>
                <h3 class="font-display text-2xl font-bold text-slate-800 mb-4">
                  Materials Selection
                </h3>
                <p class="text-slate-500 text-sm leading-relaxed">
                  Sourcing premium-grade hardwoods and architectural finishes
                  for longevity.
                </p>
              </div>
            </div>
          </div>
          <!-- Step 04 -->
          <div class="flex items-center w-full lg:w-[45%] relative reveal">
            <div
              class="group relative w-full p-8 lg:p-10 bg-white border border-slate-100 rounded-[2rem] shadow-[0_10px_40px_-15px_rgba(0,0,0,0.05)] transition-all duration-500 hover:border-primary/40 hover:shadow-2xl"
            >
              <span
                class="absolute top-4 right-8 text-6xl font-display font-bold text-primary/5 group-hover:text-primary transition-colors"
                >04</span
              >
              <div class="relative z-10">
                <div
                  class="w-14 h-14 rounded-2xl bg-slate-50 border border-slate-100 flex items-center justify-center mb-6 shadow-sm group-hover:bg-primary/10 transition-all"
                >
                  <span class="material-icons-outlined text-3xl text-primary"
                    >factory</span
                  >
                </div>
                <h3 class="font-display text-2xl font-bold text-slate-800 mb-4">
                  Manufacturing
                </h3>
                <p class="text-slate-500 text-sm leading-relaxed">
                  Precision fabrication utilizing high-end machinery and
                  artisanal techniques.
                </p>
              </div>
            </div>
          </div>
        </div>

        <!-- Row 3 (5-6) -->
        <div
          class="flex flex-col lg:flex-row justify-between gap-12 lg:gap-24 relative"
        >
          <!-- Step 05 -->
          <div class="flex items-center w-full lg:w-[45%] relative reveal">
            <div
              class="group relative w-full p-8 lg:p-10 bg-white border border-slate-100 rounded-[2rem] shadow-[0_10px_40px_-15px_rgba(0,0,0,0.05)] transition-all duration-500 hover:border-primary/40 hover:shadow-2xl"
            >
              <span
                class="absolute top-4 right-8 text-6xl font-display font-bold text-primary/5 group-hover:text-primary transition-colors"
                >05</span
              >
              <div class="relative z-10">
                <div
                  class="w-14 h-14 rounded-2xl bg-slate-50 border border-slate-100 flex items-center justify-center mb-6 shadow-sm group-hover:bg-primary/10 transition-all"
                >
                  <span class="material-icons-outlined text-3xl text-primary"
                    >construction</span
                  >
                </div>
                <h3 class="font-display text-2xl font-bold text-slate-800 mb-4">
                  Assembly
                </h3>
                <p class="text-slate-500 text-sm leading-relaxed">
                  Meticulous structural joining and rigorous quality validation
                  of the final form.
                </p>
              </div>
            </div>
          </div>
          <!-- Step 06 -->
          <div class="flex items-center w-full lg:w-[45%] relative reveal">
            <div
              class="group relative w-full p-8 lg:p-10 bg-white border border-slate-100 rounded-[2rem] shadow-[0_10px_40px_-15px_rgba(0,0,0,0.05)] transition-all duration-500 hover:border-primary/40 hover:shadow-2xl"
            >
              <span
                class="absolute top-4 right-8 text-6xl font-display font-bold text-primary/5 group-hover:text-primary transition-colors"
                >06</span
              >
              <div class="relative z-10">
                <div
                  class="w-14 h-14 rounded-2xl bg-slate-50 border border-slate-100 flex items-center justify-center mb-6 shadow-sm group-hover:bg-primary/10 transition-all"
                >
                  <span class="material-icons-outlined text-3xl text-primary"
                    >local_shipping</span
                  >
                </div>
                <h3 class="font-display text-2xl font-bold text-slate-800 mb-4">
                  Delivery & Installation
                </h3>
                <p class="text-slate-500 text-sm leading-relaxed">
                  Seamless logistical transport and precision on-site
                  architectural symphony.
                </p>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<!-- Internal Component for Step Cards -->
<script slot="head">
  // Dummy to prevent build error if needed, but Astro components work best
</script>

{/* Separate Step Markup for reuse */}
{
  (() => {
    // This is just to define the structure once
  })()
}

{/* Manual "Component" Implementation */}
<style>
  .font-display {
    font-family: "Playfair Display", serif;
  }
  .text-primary-200 {
    color: #ffd7d7;
  }

  .path-animate {
    stroke-dashoffset: 200;
    animation: flow 10s linear infinite;
  }

  @keyframes flow {
    to {
      stroke-dashoffset: 0;
    }
  }

  .reveal {
    opacity: 0;
    transform: translateY(30px);
    transition: all 1s cubic-bezier(0.22, 1, 0.36, 1);
  }
  .reveal.active {
    opacity: 1;
    transform: translateY(0);
  }
</style>

<script>
  const initProcess = () => {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add("active");
          }
        });
      },
      { threshold: 0.1 },
    );

    document.querySelectorAll(".reveal").forEach((el) => observer.observe(el));
  };

  document.addEventListener("DOMContentLoaded", initProcess);
  document.addEventListener("astro:page-load", initProcess);
</script>

{
  /* Defining Step inline since we can't easily create a separate file in one go */
}
{
  [
    /* This is just a conceptual marker, actual rendering happens in the grid above */
  ]
}

{/* ACTUAL STEP MARKUP RENDERING HELPER */}
{
  /* Since I can't use a separate file easily, I'll use a direct mapping in the grid above. 
     I will rewrite the grid sections with the actual HTML for each step. */
}
